<div class="sign">
  <div class="sign__title">
  <a href="/"><div class="about__logo"></div></a>
    <b>新規会員登録</b>
    <p>会員登録を行います。<br>情報を入力後、確認ボタンを押してください。
    </p>
  </div>
  <div class="login-main">
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <div class="row">
    <div class="col-xs-6 col-xs-offset-3">
    <%= render "devise/shared/error_messages", resource: resource %>
    </div>
    </div>
      <div class="form-group">
        <%= f.label :name %>　　<i class="fas fa-pencil-alt">　</i>
        <%= f.text_field :name, autofocus: true, autocomplete: "name",placeholder:" ニックネームを入力してください",class: 'form-group__input' %>
      </div>
      <div class="form-group">
        <%= f.label :メールアドレス %>　<i class="fas fa-envelope">　</i>
        <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "sample@stores.jp",class: 'form-group__input' %>
      </div>
      <div class="form-group">
        <%= f.label :password %>　　　<i class="fas fa-key">　</i>
        <%= f.password_field :password,autocomplete: "new-password",placeholder: "6文字以上の半角英数字",class:'form-group__input',id:"password" %>
      </div>
      <div class="form-group">
        <%= f.label :password_confirmation %>
        <%= f.password_field :password_confirmation, autocomplete: "new-password",placeholder:" もう一度パスワードを入力してください", class:'form-group__input'%>
      </div>

      <div class="action">
        <%= f.submit "確認", class: "btn btn-primary btn-lg btn-block" %>
    <% end %>   
        <div class="action__link">
          <%= link_to "既に登録済みの方はこちらから", new_session_path(resource_name) %>
        </div>
      </div>
  </div>
</div>

